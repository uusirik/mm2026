name: Fetch Match Results

on:
  schedule:
    - cron: '*/5 * * * *'   # joka 5. minuutti
  workflow_dispatch:          # voi ajaa käsin

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Hae tulokset ja kertoimet (ESPN + Veikkaus)
        env:
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          VEIKKAUS_USERNAME: ${{ secrets.VEIKKAUS_USERNAME }}
          VEIKKAUS_PASSWORD: ${{ secrets.VEIKKAUS_PASSWORD }}
        run: node scripts/fetch-results.js
